- hosts: localhost
  become: true
  tasks:
    - name: Install btop
      ansible.builtin.apk:
        name: btop
        state: present
    - name: Install neovim
      ansible.builtin.apk:
        name: neovim 
        state: present
    - name: Install neovim Doc # Alpine separeate neovim from neovim-doc
      ansible.builtin.apk:
        name: neovim-doc 
        state: present
    - name: Install curl 
      ansible.builtin.apk:
        name: curl 
        state: present
